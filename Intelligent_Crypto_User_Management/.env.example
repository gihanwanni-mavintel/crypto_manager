# Environment Variables Template
# Copy this file to .env and fill in your actual values
# NEVER commit .env to git - it contains sensitive credentials

# Server Configuration
PORT=8081

# Database Configuration (Neon PostgreSQL)
DB_URL=jdbc:postgresql://your-host.neon.tech:5432/neondb?sslmode=require
DB_USERNAME=your_username
DB_PASSWORD=your_password

# Binance API Configuration
# Get these from: https://www.binance.com/en/my/settings/api-management
BINANCE_API_KEY=your_binance_api_key
BINANCE_API_SECRET=your_binance_api_secret

# Trading Mode
# Set to 'true' for testnet (safe testing), 'false' for live trading (REAL MONEY)
BINANCE_TESTNET_ENABLED=false

# Trading Configuration
# Auto-execute trades when signals are received
TRADE_AUTO_EXECUTE=true

# Maximum number of concurrent trades
TRADE_MAX_CONCURRENT=5

# Default position size as percentage of balance
TRADE_POSITION_SIZE=50

# Minimum USDT balance required to execute trades
TRADE_MIN_BALANCE=10

# Minimum signal confidence (0.0-1.0) to execute trade
TRADE_MIN_CONFIDENCE=0.8

# ==========================================
# PRODUCTION DEPLOYMENT NOTES
# ==========================================
#
# SECURITY CHECKLIST:
# ✓ Use IP restrictions on Binance API key
# ✓ Enable only required permissions (Futures Trading)
# ✓ Start with testnet (BINANCE_TESTNET_ENABLED=true)
# ✓ Test with small amounts first
# ✓ Never share or commit this file
# ✓ Use strong database password
#
# BINANCE API PERMISSIONS REQUIRED:
# - Enable Spot & Margin Trading: NO
# - Enable Futures: YES
# - Enable Reading: YES
#
# RECOMMENDED SETTINGS FOR PRODUCTION:
# - TRADE_MIN_BALANCE: 10-50 (depending on your capital)
# - TRADE_POSITION_SIZE: 10-50 (start conservative)
# - TRADE_MAX_CONCURRENT: 3-5 (to limit risk)
# - TRADE_MIN_CONFIDENCE: 0.8 (only high-confidence signals)
#
# ==========================================
